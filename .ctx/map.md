---
describes: ["."]
verified: <YYYY-MM-DD>
verified_sha: <commit>
---

# Map

Where things live. The highest return-per-token document in this folder: twenty
honest lines here save an agent from reading two thousand lines of source.

Regenerate or re-verify this whenever the top-level layout changes.

## Layout

<!-- FILL. Say what each directory is *for*, and any rule about what may not go
     in it. The constraint is worth more than the description. -->

- `<FILL: src/api/>` — <purpose>. <constraint, e.g. "thin; no business logic">
- `<FILL: src/core/>` — <purpose>. <constraint, e.g. "pure; no I/O">
- `<FILL: src/db/>` — <purpose>. <constraint, e.g. "read its README first">
- `<FILL: tests/>` — <purpose>. <constraint>

## Entry points

<!-- Where execution actually begins. Agents waste real time hunting for these. -->

- App / server: `<FILL>`
- CLI: `<FILL>`
- Background worker: `<FILL>`
- Tests: `<FILL>`

## Where to look for common concerns

<!-- Task-oriented, not structure-oriented. This is what gets used most. -->

| Concern | Start here |
|---|---|
| Auth / sessions | `<FILL>` |
| Config and env vars | `<FILL>` |
| Database access | `<FILL>` |
| External API clients | `<FILL>` |
| Error handling / logging | `<FILL>` |
| Shared types | `<FILL>` |

## Boundaries that matter

<!-- FILL: the rules that keep the codebase coherent. Examples:
     - `core/` must not import from `api/`. Dependencies point inward.
     - Nothing outside `db/` may construct a raw query.
     - All third-party calls go through a client in `clients/`. -->

- <FILL>

## Do not touch

<!-- Generated, vendored, or otherwise off-limits. Duplicated in core.md
     because it's the single most damage-preventing list here. -->

- `<FILL>` — <why: generated by `<command>` / vendored / applied migrations>

## Naming and file conventions

<!-- FILL: e.g. one component per file, PascalCase filenames for components,
     tests co-located as `*.test.ts`, no index.ts barrels. -->

- <FILL>
