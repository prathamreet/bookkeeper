# Copilot instructions

See **[AGENTS.md](../AGENTS.md)** — the single entry point for every tool. Read
it in full: it carries the standing bookkeeping obligations and the hard stops,
which apply to every session.

Project context lives in `.ctx/`. Start with `.ctx/core.md`, then
`.ctx/handoff.md`. `.ctx/index.md` is the inventory of everything else.

<!-- Copy to .github/copilot-instructions.md — the relative link above assumes
     that location. Deliberately a pointer, not a copy. -->
