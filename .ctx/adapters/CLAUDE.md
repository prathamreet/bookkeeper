# CLAUDE.md

See **[AGENTS.md](AGENTS.md)** — the single entry point for every tool. Read it
in full, not as a signpost: it carries the standing bookkeeping obligations and
the hard stops, which apply to every session.

Project context lives in `.ctx/`. Start with `.ctx/core.md`, then
`.ctx/handoff.md`. `.ctx/index.md` is the inventory of everything else.

<!-- Deliberately a pointer, not a copy. Duplicated instructions drift, and a
     context file that lies is worse than one that's missing. See
     "Why it's built this way" in .ctx/README.md -->
